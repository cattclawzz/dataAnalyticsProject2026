pip install: flask, pandas, matplotlib, animation, os, time

Open "run.bat", this will run the server and open the local host in a browser window
If run.bat fails, replace "py" on line 2 with "python"/"python3"/"py3"/other depending on the machine